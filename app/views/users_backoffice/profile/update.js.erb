$('#avatarModal').modal('hide');

document.querySelector('#AvatarImgLG').src = '<%= url_for @user.user_profile.avatar %>'
document.querySelector('#AvatarImgSM').src = '<%= url_for @user.user_profile.avatar %>'
document.querySelector('#AvatarImgModal').src = '<%= url_for @user.user_profile.avatar %>'


$.bootstrapGrowl('Avatar updated successfully!', {
  type: 'success', // (null, 'info', 'danger', 'success')
  align: 'right', // ('left', 'right', or 'center')
  allow_dismiss: true, // If true then will display a cross to close the popup.
  stackup_spacing: 10 // spacing between consecutively stacked growls.
});
